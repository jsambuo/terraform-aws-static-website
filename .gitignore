# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*.backup

# Crash log files
crash.log

# Exclude all .tfvars files, which might contain sensitive data, such as
# passwords, private keys, and other secrets. These should not be part of version control
# as they are project-specific.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally.
override.tf
override.tf.json
*.override.tf
*.override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
